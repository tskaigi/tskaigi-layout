import type { FC } from "react";

import { render } from "../../render";

import { Base } from "../components/Base";

const App: FC = () => {
  return <Base></Base>;
};

render(
  <>
    <App />
  </>,
);
